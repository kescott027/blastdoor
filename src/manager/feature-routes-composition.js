import { registerRemoteSupportRoutes } from "./remote-support-routes.js";
import { registerDiagnosticsRoutes } from "./diagnostics-routes.js";
import { registerManagerAuthRoutes } from "./auth-routes.js";
import { registerManagerServiceRoutes } from "./service-routes.js";
import { registerManagerOperationsRoutes } from "./operations-routes.js";
import { registerManagerUserRoutes } from "./users-routes.js";
import { registerManagerThemeRoutes } from "./themes-routes.js";
import { registerManagerConfigRoutes } from "./config-routes.js";

export function registerManagerFeatureRoutes(options = {}) {
  const {
    registerApiGet,
    registerApiPost,
    registerRemoteSupportGet,
    registerRemoteSupportPost,
    readConsoleSettings,
    writeConsoleSettings,
    getManagerAuthSession,
    normalizeManagerNextPath,
    normalizeString,
    verifyPassword,
    createManagerAuthSession,
    createCookieHeader,
    managerAuthCookieName,
    clearManagerAuthSession,
    renderManagerLoginPage,
    sanitizeManagerConsoleSettingsForClient,
    normalizeManagerConsoleSettings,
    parseBooleanLikeBody,
    createPasswordHash,
    readEnvConfig,
    envPath,
    scrubConfigForClient,
    detectEnvironmentInfo,
    workspaceDir,
    detectWslDefaultGatewayIp,
    commandRunner,
    buildWslFoundryTarget,
    checkFoundryTargetHealth,
    probeFoundryApiStatus,
    buildWslOllamaUrl,
    probeHttpHealth,
    parseBodyConfig,
    sensitiveConfigKeys,
    parseBooleanLike,
    createSessionSecret,
    validateConfig,
    loadConfigFromEnv,
    writeEnvConfig,
    writeBlastDoorsState,
    runtimeStatePath,
    configDefaults,
    processState,
    configBackupService,
    configBackupDir,
    validateConfigBackupId,
    detectTlsEnvironment,
    normalizeTlsChallengeMethod,
    normalizeTlsConfigBody,
    buildLetsEncryptPlan,
    validateGatewayStartConfiguration,
    recordFailureEntry,
    withBlastdoorApi,
    createSessionKey,
    validateManagedUsernameForActions,
    safeEqual,
    normalizeUserFilter,
    buildManagedUserList,
    validateManagedUsername,
    normalizeUserStatus,
    sanitizeLongText,
    sanitizeEmail,
    createEmailService,
    loadEmailConfigFromEnv,
    resolveGatewayBaseUrl,
    getControlPlaneStatusCached,
    readFailureStore,
    summarizeFailureStore,
    clearFailureStore,
    failureStorePath,
    checkBlastdoorHealth,
    tailFile,
    mapThemeForClient,
    validateThemeAssetSelection,
    createThemeId,
    normalizeThemeName,
    defaultThemeId,
    buildDiagnosticsPayload,
    buildTroubleshootPayload,
    runTroubleshootAction,
    controlPlaneCache,
    managerOperationTimeoutMs,
    clampRemoteSupportTokenTtlMinutes,
    remoteSupportTokenMinTtlMinutes,
    remoteSupportTokenMaxTtlMinutes,
    remoteSupportDefaultTokenLabel,
    trimCallHomeEvents,
    summarizeRemoteSupportToken,
    callHomeEventsMax,
    syncRemoteSupportWslExposure,
    randomBytes,
    randomUUID,
    buildRemoteSupportCurlExamples,
    buildCallHomePodBundle,
    buildRemoteSupportApiBasePath,
    appendCallHomeEvent,
    buildRemoteSupportCommandHints,
    remoteSupportSafeActionAllowlist,
    readIntelligenceAgentStore,
    intelligenceAgentStorePath,
    pluginManager,
    sanitizeConfigForDiagnostics,
    createTroubleshootReport,
    applyThreatLockdown,
    installationConfigPath,
  } = options;

  registerManagerAuthRoutes({
    registerApiGet,
    registerApiPost,
    readConsoleSettings,
    getManagerAuthSession,
    normalizeManagerNextPath,
    normalizeString,
    verifyPassword,
    createManagerAuthSession,
    createCookieHeader,
    managerAuthCookieName,
    clearManagerAuthSession,
    renderManagerLoginPage,
  });

  registerManagerConfigRoutes({
    registerApiGet,
    registerApiPost,
    readConsoleSettings,
    writeConsoleSettings,
    sanitizeManagerConsoleSettingsForClient,
    normalizeManagerConsoleSettings,
    parseBooleanLikeBody,
    normalizeString,
    createPasswordHash,
    readEnvConfig,
    envPath,
    scrubConfigForClient,
    detectEnvironmentInfo,
    workspaceDir,
    detectWslDefaultGatewayIp,
    commandRunner,
    buildWslFoundryTarget,
    checkFoundryTargetHealth,
    probeFoundryApiStatus,
    buildWslOllamaUrl,
    probeHttpHealth,
    parseBodyConfig,
    sensitiveConfigKeys,
    parseBooleanLike,
    createSessionSecret,
    validateConfig,
    loadConfigFromEnv,
    writeEnvConfig,
    writeBlastDoorsState,
    runtimeStatePath,
    configDefaults,
    processState,
    listConfigBackups: configBackupService.listBackups,
    configBackupDir,
    validateConfigBackupId,
    viewConfigBackup: configBackupService.viewBackup,
    createConfigBackup: configBackupService.createBackup,
    restoreConfigBackup: configBackupService.restoreBackup,
    deleteConfigBackup: configBackupService.deleteBackup,
    cleanInstallConfiguration: configBackupService.cleanInstall,
    detectTlsEnvironment,
    normalizeTlsChallengeMethod,
    normalizeTlsConfigBody,
    buildLetsEncryptPlan,
    accessFile: options.accessFile,
    resolvePath: options.resolvePath,
  });

  registerManagerServiceRoutes({
    registerApiGet,
    registerApiPost,
    validateGatewayStartConfiguration,
    processState,
    recordFailureEntry,
    readEnvConfig,
    envPath,
    createSessionSecret,
    writeEnvConfig,
    withBlastdoorApi,
    createSessionKey,
    validateManagedUsernameForActions,
    safeEqual,
  });

  registerManagerUserRoutes({
    registerApiGet,
    registerApiPost,
    normalizeUserFilter,
    buildManagedUserList,
    validateManagedUsername,
    validateManagedUsernameForActions,
    normalizeString,
    normalizeUserStatus,
    sanitizeLongText,
    sanitizeEmail,
    withBlastdoorApi,
    createPasswordHash,
    createEmailService,
    loadEmailConfigFromEnv,
    resolveGatewayBaseUrl,
  });

  registerManagerOperationsRoutes({
    registerApiGet,
    registerApiPost,
    getControlPlaneStatusCached,
    readFailureStore,
    summarizeFailureStore,
    clearFailureStore,
    failureStorePath,
    processState,
    readEnvConfig,
    envPath,
    checkBlastdoorHealth,
    workspaceDir,
    configDefaults,
    tailFile,
  });

  registerManagerThemeRoutes({
    registerApiGet,
    registerApiPost,
    withBlastdoorApi,
    mapThemeForClient,
    validateThemeAssetSelection,
    parseBooleanLikeBody,
    createThemeId,
    normalizeString,
    normalizeThemeName,
    defaultThemeId,
  });

  registerDiagnosticsRoutes({
    registerApiGet,
    registerApiPost,
    buildDiagnosticsPayload,
    buildTroubleshootPayload,
    normalizeString,
    readEnvConfig,
    detectEnvironmentInfo,
    envPath,
    workspaceDir,
    runTroubleshootAction,
    commandRunner,
    controlPlaneCache,
    operationTimeoutMs: managerOperationTimeoutMs,
  });

  registerRemoteSupportRoutes({
    registerApiGet,
    registerApiPost,
    registerRemoteSupportGet,
    registerRemoteSupportPost,
    normalizeString,
    parseBooleanLike,
    readConsoleSettings,
    writeConsoleSettings,
    clampRemoteSupportTokenTtlMinutes,
    remoteSupportTokenMinTtlMinutes,
    remoteSupportTokenMaxTtlMinutes,
    remoteSupportDefaultTokenLabel,
    trimCallHomeEvents,
    summarizeRemoteSupportToken,
    callHomeEventsMax,
    syncRemoteSupportWslExposure,
    detectEnvironmentInfo,
    workspaceDir,
    envPath,
    commandRunner,
    randomBytes,
    randomUUID,
    createPasswordHash,
    buildRemoteSupportCurlExamples,
    buildCallHomePodBundle,
    buildRemoteSupportApiBasePath,
    appendCallHomeEvent,
    buildDiagnosticsPayload,
    buildRemoteSupportCommandHints,
    buildTroubleshootPayload,
    remoteSupportSafeActionAllowlist,
    readEnvConfig,
    runTroubleshootAction,
    withBlastdoorApi,
    readIntelligenceAgentStore,
    intelligenceAgentStorePath,
    operationTimeoutMs: managerOperationTimeoutMs,
  });

  registerApiGet("/plugins/ui", async (_req, res) => {
    res.json({
      ok: true,
      plugins: pluginManager.getManagerUiAssets(),
    });
  });

  pluginManager.registerManagerRoutes({
    registerApiGet,
    registerApiPost,
    readEnvConfig,
    withBlastdoorApi,
    processState,
    workspaceDir,
    envPath,
    checkBlastdoorHealth,
    checkFoundryTargetHealth,
    detectEnvironmentInfo,
    sanitizeConfigForDiagnostics,
    createTroubleshootReport,
    tailFile,
    parseBooleanLike,
    parseBooleanLikeBody,
    normalizeString,
    applyThreatLockdown,
    runTroubleshootAction,
    commandRunner,
    CONFIG_DEFAULTS: configDefaults,
    installationConfigPath,
  });
}
